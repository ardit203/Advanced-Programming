Write a generic method `entriesSortedByValues` for sorting the entries (key–value pairs) of a map by the value in **descending** order.
If there are two or more equal values, the order defined in the map should be preserved.
The sorted entries of the map should be returned as a `SortedSet<Map.Entry<, >>`.