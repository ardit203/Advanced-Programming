Map<String, Integer> sorted =
        participants.entrySet().stream()
                .sorted(Map.Entry.<String, Integer>comparingByValue().reversed())
                .collect(java.util.stream.Collectors.toMap(
                        Map.Entry::getKey,
                        Map.Entry::getValue,
                        (a, b) -> a,              // merge func (won’t happen here)
                        java.util.LinkedHashMap::new
                ));


//        Map<String, Set<Book>> groupByCategory = books.stream()
//                .collect(Collectors.groupingBy(
//                                Book::getCategory,
//                                TreeMap::new,
//                                Collectors.toCollection(
//                                        () -> new TreeSet<>(
//                                                Comparator.comparing(Book::getTitle)
//                                                        .thenComparing(Book::getPrice)
//                                        )
//                                )
//                        )
//                );

//        Map<String, Set<Book>> groupByCategory = new TreeMap<>();

//        for (int i = 0; i < books.size(); i++) {
//            Book book = books.get(i);
//            groupByCategory.computeIfAbsent(book.getCategory(), k -> new TreeSet<>(comparator)).add(book);
//        }
//
//        groupByCategory.forEach((key, value) -> {
//            System.out.println("CATEGORY: " + key);
//            value.forEach(System.out::println);
//        });